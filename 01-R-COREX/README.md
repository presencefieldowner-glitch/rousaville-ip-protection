# 01-R-CoreX

The parent architecture. Per `RT-IP-RCOREX-MAPPING-002` Section 3, R-CoreX is not a separate codebase — it is the abstract description of `presencefieldowner-glitch/RealityStateOS`'s own architecture. See that repository's `README.md` module map and `000_ARCHITECTURE/` for the real, current source of truth.

- [`architecture/`](architecture/) — points to RealityStateOS's module map and Build Order.
- [`specifications/`](specifications/) — points to RealityStateOS's RFC-0002 through RFC-0006.
- [`diagrams/`](diagrams/) — the dossier's own ASCII architecture trees (Sections 3, 12, 29).
