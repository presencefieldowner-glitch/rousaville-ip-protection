# 08-Telemetry & Event Fabric

Real implementation is spread across `RealityStateOS/003_FOUNDATION` (schemas), `004_VERIFIED_BUS` (transport), `005_KNOWLEDGE` and `007_PRESENCE` (provenance/identity), `999_OPERATIONS` (audit/monitoring). See each subfolder for exact paths.
